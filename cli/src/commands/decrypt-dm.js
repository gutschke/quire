// quire decrypt-dm: decrypt previously-encrypted dm/ subfolders so the DM can
// edit them locally.  Counterpart to `encrypt-dm`.
//
// Phase 0 stub — see `encrypt-dm.js` for the intended pipeline.

export async function run(args) {
  console.error('quire decrypt-dm: not yet implemented (phase 0 stub).');
  console.error('Decryption tooling lands in phase 1.  See src/commands/decrypt-dm.js');
  console.error('for the intended behaviour.');
  process.exit(64);
}
